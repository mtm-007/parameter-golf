# numpy
# tqdm
# torch==2.3.1
# flash-attn==2.7.3
# huggingface-hub
# kernels
# setuptools
# typing-extensions==4.15.0
# datasets
# tiktoken
# sentencepiece
# wandb
# PyTorch (must be installed first - CUDA 12.1 is the correct index for torch 2.3.1)


#------------------
# torch==2.3.1
# torchvision==0.18.1
# torchaudio==2.3.1

## ----------------Flash-attn 3 dependencies---------------
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Flash Attention 3 (for H100)
flash-attn==2.7.3

# Build dependencies (very important for flash-attn compilation)
ninja
packaging
wheel
setuptools
psutil

# Your other original packages
numpy
tqdm
huggingface-hub
datasets
tiktoken
sentencepiece
wandb

# Typing
typing-extensions==4.15.0


## ----------uv step by step set up for flash-attn 3-----------
# # 1. Install PyTorch first (this is the key fix)
#uv pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu128

# # 2. Install build dependencies
# uv pip install ninja packaging wheel setuptools psutil

# # 3. Install flash-attn with no build isolation (this resolves the "No module named 'torch'" error)
# uv pip install flash-attn==2.7.3 --no-build-isolation

# # 4. Install the rest
# uv pip install -r requirements.txt --no-deps